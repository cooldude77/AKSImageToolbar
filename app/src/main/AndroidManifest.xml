<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.instanect.aksimagetoolbarapp">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.instanect.aksimagetoolbaractivity.activity.ImageToolbarActivity"
            android:theme="@style/Theme.AppCompat.Light">
            <intent-filter>
                <!-- This Activity's name will be used for shortcut on desktop
                http://stackoverflow.com/questions/2444040/naming-my-application-in-android -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>
</manifest>
